## project for up postgresql DB in docker compose.

#### get start.

For start change ```volumse``` place in ur local repasitory, in docker compose file.
```
    volumes:
      - /home/pydev/Desktop/tester/postgresql_docker/vol:/var/lib/postgresql/data
```
